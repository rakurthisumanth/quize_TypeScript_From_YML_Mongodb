import { OpenAPI } from 'openapi-types';
import { CodegenLiteralValueOptions, CodegenGenerator } from '@openapi-generator-plus/types';
import { CodegenSpecVersion } from './types';
export declare function toSpecVersion(root: OpenAPI.Document): CodegenSpecVersion;
export declare function stringLiteralValueOptions(generator: CodegenGenerator): CodegenLiteralValueOptions;
export declare type CompareFunction<T> = (a: T, b: T) => number;
/**
 * Chain CompareFunctions for multi-factor sorting.
 * @param compares
 */
export declare function chainedCompare<T>(...compares: CompareFunction<T>[]): CompareFunction<T>;
/**
 * Test whether the given string looks like a URL according to https://www.ietf.org/rfc/rfc1738.txt
 * <p>
 * Checks if the string starts with a valid scheme followed by a colon.
 * @param value
 * @returns
 */
export declare function isURL(value: string): boolean;
