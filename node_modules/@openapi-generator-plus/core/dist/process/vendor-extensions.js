"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.mergeCodegenVendorExtensions = exports.toCodegenVendorExtensions = void 0;
// eslint-disable-next-line @typescript-eslint/ban-types
function toCodegenVendorExtensions(ob) {
    const result = {};
    let found = false;
    for (const name in ob) {
        if (name.startsWith('x-')) {
            result[name] = ob[name];
            found = true;
        }
    }
    return found ? result : null;
}
exports.toCodegenVendorExtensions = toCodegenVendorExtensions;
/**
 * Merges two CodegenVendorExtensions, with those in the second argument overwriting any duplicates in the first.
 * @param a
 * @param b
 */
function mergeCodegenVendorExtensions(a, b) {
    if (!a) {
        return b;
    }
    else if (!b) {
        return a;
    }
    else {
        const result = Object.assign(Object.assign({}, a), b);
        return result;
    }
}
exports.mergeCodegenVendorExtensions = mergeCodegenVendorExtensions;
