"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.isMultipart = exports.toCodegenMediaType = void 0;
function toCodegenMediaType(mediaType) {
    const i = mediaType.indexOf(';');
    if (i === -1) {
        return {
            mediaType,
            mimeType: mediaType,
            encoding: null,
        };
    }
    let charset;
    const matches = mediaType.match(/charset\s*=\s*([-a-zA-Z0-9_]+)/i);
    if (matches) {
        charset = matches[1];
    }
    return {
        mediaType,
        mimeType: mediaType.substring(0, i),
        encoding: charset || null,
    };
}
exports.toCodegenMediaType = toCodegenMediaType;
function isMultipart(mediaType) {
    return mediaType.mimeType.startsWith('multipart/');
}
exports.isMultipart = isMultipart;
