import { CodegenHeaders } from '@openapi-generator-plus/types';
import { InternalCodegenState } from '../types';
import { OpenAPIX } from '../types/patches';
export declare function toCodegenHeaders(headers: OpenAPIX.Headers | undefined, state: InternalCodegenState): CodegenHeaders | null;
