import { CodegenHierarchySchema, CodegenInterfaceSchema, CodegenObjectSchema, CodegenScope } from '@openapi-generator-plus/types';
import { InternalCodegenState } from '../../types';
/**
 * Create or return an interface schema for the given object schema
 * @param schema
 * @returns
 */
export declare function toCodegenInterfaceSchema(schema: CodegenObjectSchema | CodegenHierarchySchema, scope: CodegenScope | null, state: InternalCodegenState): CodegenInterfaceSchema;
interface InterfaceImplementationOptions {
    /**
     * `true` if the implementation could be an abstract implementation
     */
    allowAbstract: boolean;
    suggestedName?: string;
}
/**
 * Create or return the implementation schema for the given interface schema.
 * @param schema
 * @param suggestedAbstract `true` if the implementation could be an abstract implementation
 * @returns a CodegenObjectSchema, or undefined if an implementation cannot be created
 */
export declare function toCodegenInterfaceImplementationSchema(interfaceSchema: CodegenInterfaceSchema, options: InterfaceImplementationOptions, state: InternalCodegenState): CodegenObjectSchema | undefined;
export {};
