import { CodegenArraySchema, CodegenSchemaUsage, CodegenScope } from '@openapi-generator-plus/types';
import { InternalCodegenState } from '../../types';
import { OpenAPIX } from '../../types/patches';
import { ScopedModelInfo } from './naming';
export declare function toCodegenArraySchema(apiSchema: OpenAPIX.SchemaObject, naming: ScopedModelInfo | null, suggestedItemModelName: string, suggestedItemModelScope: CodegenScope | null, state: InternalCodegenState): CodegenArraySchema;
/**
 * Create a new schema of an array type with the given name, in the given scope, and add it to that scope.
 * @param scope the scope in which to create the object, or `null` to create in the global scope
 * @param state
 * @returns
 */
export declare function createArraySchema(component: CodegenSchemaUsage, state: InternalCodegenState): CodegenArraySchema;
