import { CodegenObjectLikeSchemas, CodegenObjectSchema, CodegenSchema, CodegenScope } from '@openapi-generator-plus/types';
import { OpenAPIX } from '../../types/patches';
import { InternalCodegenState } from '../../types';
export declare function absorbCodegenSchema(schema: CodegenObjectLikeSchemas, target: CodegenObjectSchema, options?: {
    includeNestedSchemas?: boolean;
    makePropertiesOptional?: boolean;
}): void;
export declare function absorbApiSchema(apiSchema: OpenAPIX.SchemaObject, target: CodegenObjectSchema, scope: CodegenScope | null, state: InternalCodegenState): CodegenSchema | undefined;
