"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.toCodegenInfo = void 0;
function toCodegenInfo(info) {
    return {
        title: info.title,
        description: info.description || null,
        termsOfService: info.termsOfService || null,
        contact: info.contact ? toCodegenContact(info.contact) : null,
        license: info.license ? toCodegenLicense(info.license) : null,
        version: info.version,
    };
}
exports.toCodegenInfo = toCodegenInfo;
function toCodegenContact(contact) {
    return {
        name: contact.name || null,
        email: contact.email || null,
        url: contact.url || null,
    };
}
function toCodegenLicense(license) {
    return {
        name: license.name,
        url: license.url || null,
    };
}
