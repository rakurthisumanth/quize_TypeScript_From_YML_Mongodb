import { CodegenOperation, CodegenParameters, CodegenServer, CodegenVendorExtensions } from '@openapi-generator-plus/types';
import { OpenAPI } from 'openapi-types';
import { InternalCodegenState } from '../types';
export interface CodegenOperationContext {
    parameters?: CodegenParameters;
    summary?: string;
    description?: string;
    vendorExtensions: CodegenVendorExtensions | null;
    servers: CodegenServer[] | null;
}
export declare function toCodegenOperation(fullPath: string, method: string, operation: OpenAPI.Operation, context: CodegenOperationContext, state: InternalCodegenState): CodegenOperation;
